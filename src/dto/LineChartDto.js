/**
 * @class LineChartDto - Represents a data transfer object for a line chart.
 */
class LineChartDto {
  /**
   * Create a LineChartDto.
   * @param {Array} data - The data for the line chart.
   */
  constructor(data) {
    this.data = data;
  }
}

export default LineChartDto;
