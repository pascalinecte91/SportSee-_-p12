/**
 * @class RadarPerformanceDto - Represents a data transfer object for radar performance.
 */
class RadarPerformanceDto {
  /**
   * Create a RadarPerformanceDto.
   * @param {Array} data - The data for radar performance.
   */
  constructor(data) {
    this.data = data;
  }
}

export default RadarPerformanceDto;
